# Bevy development optimizations
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=lld"]

# Enable more aggressive optimizations during compilation
[build]
# Use multiple cores for parallel compilation (auto-detect available cores)
# Remove this line to let Cargo auto-detect optimal job count
